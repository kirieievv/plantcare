.DS_Store
.cursor/
firebase-debug.log

# Local workspace copies
plant_care_clean/

# Dev app build artifacts and local caches (the app itself IS tracked)
plant_care_dev/functions/node_modules/
plant_care_dev/.firebase/
plant_care_dev/firepit-log.txt

# Local secrets and recovery files
twilio_2FA_recovery_code.txt
plant-care-*.json
*service-account*.json

# Firebase local cache artifacts
plant_care/.firebase/hosting.*.cache
